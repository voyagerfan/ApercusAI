# ApercusAI

## Testing
### Backend
- To run the backend testing suite:
  - From the terminal, ensure the current directory is `backend`. (e.g. from the root directory, `cd backend`)
  - next, simply run the command `pytest`